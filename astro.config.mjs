// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://sinester09.github.io',
  base: '/landing-armeria', // Importante: debe coincidir con el nombre del repositorio
});
